export default function groceriesList() {
  const grociers = new Map();

  grociers.set('Apples', 10);
  grociers.set('Tomatoes', 10);
  grociers.set('Pasta', 1);
  grociers.set('Rice', 1);
  grociers.set('Banana', 5);

  return grociers;
}
